{% extends 'base.html.twig' %}

{% block title %}Librarysection{% endblock %}

{% block body %}
    <h1>Librarysection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ librarysection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ librarysection.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('librarysection_index') }}">back to list</a>

    <a href="{{ path('librarysection_edit', {'id': librarysection.id}) }}">edit</a>

    {{ include('librarysection/_delete_form.html.twig') }}
{% endblock %}